void main() {
  List emptyList = [];
  List numbers = [12, 3, 6, 7, 9, 38];
  if (numbers[0] % 2 == 0) {
    emptyList.add('True');
  } else {
    emptyList.add('False');
  }
  if (numbers[1] % 2 == 0) {
    emptyList.add('True');
  } else {
    emptyList.add('False');
  }
  if (numbers[2] % 2 == 0) {
    emptyList.add('True');
  } else {
    emptyList.add('False');
  }
  if (numbers[3] % 2 == 0) {
    emptyList.add('True');
  } else {
    emptyList.add('False');
  }
  if (numbers[4] % 2 == 0) {
    emptyList.add('True');
  } else {
    emptyList.add('False');
  }
  if (numbers[5] % 2 == 0) {
    emptyList.add('True');
  } else {
    emptyList.add('False');
  }
  print(numbers);
  print(emptyList);
}
